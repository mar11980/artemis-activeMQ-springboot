spring:
  application:
    name: activemq.messaging

  artemis:
    mode: native
    broker-url: tcp://localhost:61616
    user: admin
    password: admin

  jms:
    listener:
      auto-startup: true
      concurrency: 1

server:
  port: 8080